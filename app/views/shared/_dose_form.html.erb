
<%=  "-----------------------------" %>
<!-- Need to precise the method! -->

<%= simple_form_for ([@cocktail, @dose]) do |f| %>
  <%#= f.association :ingredient, input_html: { class: 'select2' } %>
  <%= select_tag :ingredient,options_from_collection_for_select(@ingredients, :id, :name, "1") %>
  <%#= f.association select_tag :ingredient, options_for_select(Ingredient.all.map {|x| [x.name, x.id]}), class: "select2" %>

  <%#= select_tag :ingredient, options_from_collection_for_select(Ingredient.all,
    :name, :name) %>
  <%#= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.button :submit, "Add dose", class: "btn btn-success" %>
<% end %>





<!-- NE PAS TOUCHER IL MARCH!!!!!!!!!!!!!! -->
<%#= simple_form_for ([@cocktail, @dose]), method: :post do |f| %>
        <%#= f.association :ingredient, class: 'select2' %>
        <%#= f.input :description %>
        <%#= f.button :submit, class: "btn btn-primary"%>
      <%# end %>

