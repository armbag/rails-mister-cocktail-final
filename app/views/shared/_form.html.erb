<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.input :remote_photo_url, label: "OR photo URL" %>
  <%= f.submit 'Submit!' %>
<% end %>

